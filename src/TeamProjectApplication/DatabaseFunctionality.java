package TeamProjectApplication;

public interface DatabaseFunctionality {

	//abstract method for adding to database
	void addToDatabase();
	
	//abstrace method for deleteing from database
	void deleteFromDatabase(String identifier);
}
